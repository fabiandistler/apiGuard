% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_api}
\alias{check_api}
\title{CI gate: fail if the API changed in a forbidden way}
\usage{
check_api(old, new, fail_on = c("breaking", "behaviour"))
}
\arguments{
\item{old, new}{Snapshots or paths, as for \code{\link[=compare_api]{compare_api()}}.}

\item{fail_on}{Severities that should fail the check.}
}
\value{
Invisibly, the \code{api_diff} (when the check passes).
}
\description{
Designed to be called in CI right after a release tag is created (or in a
PR against the last release tag):
}
\details{
\if{html}{\out{<div class="sourceCode sh">}}\preformatted{Rscript -e 'apiGuard::check_api(apiGuard::api_snapshot_ref("v1.0.0"), ".")'
}\if{html}{\out{</div>}}

Exits non-zero (via \code{stop()}) when any change of a severity listed in
\code{fail_on} is detected.
}
